import React from "react"
import { ComponentPlaceholder } from "@michael/gatsby-theme-core"

const PostTeaser = () => <ComponentPlaceholder foo="bar" path={__filename} />;

export default PostTeaser;
